# Dependencies
node_modules/

# Environment variables
.env
.env.local
.env.*.local

# Build outputs
dist/
build/

# OS files
.DS_Store

# TypeScript
*.tsbuildinfo

# Debug
npm-debug.log*

# Client-provided reference docs (not for the repo)
Kifaayat_3.0_Schema_Migration.docx
Kifaayat_3.0_tables.pdf
Kifaayat_3.pdf
Kifaayat_3.0_tables.txt
Kifaayat_3.txt
Kifayat.csv

# Migration / Sharetribe source data (large, contains real or synthetic user data)
rawdata_synthetic.json
rawdata_synthetic_new.json
designer_full_export.csv
designer_full_export.json

# Database dumps (real prod data, never commit). Anchored to repo root
# so pg_dump output (data.sql, full-backup.sql, roles.sql, schema.sql,
# main_data.sql, old_db_dump.sql) is ignored WITHOUT touching the
# tracked schema files under src/db/.
/*.sql
*.sql.bak
*.sql.dump

# Client-provided Sharetribe field-mapping reference (PDFs/exports)
sharetribe-data/

# Photo samples / bundles (binary, large)
sample-da/
photo-bundle/
bundle-report.json

# Screenshots / scratch images in repo root
/*.png
/*.jpg
/*.jpeg
/*.heic
/*.webp

# Editor / tooling
.vscode/
.idea/
*.swp


Kifaayat_Notifications_Workflow.docx.pdf

images
images 2
images 3
images_all

Data Mapping 01 July 2026.pdf
data_synthetic.json
designer clean up - designer-brand.csv
manifest.json
WORK-LOG-2026-07-02.md
